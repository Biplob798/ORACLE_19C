SELECT
    p.PRODUCT_ID,
    p.PRODUCT_NAME,
    MIN(NVL(oi.QUANTITY,0)) AS min_quantity,
    MAX(NVL(oi.QUANTITY,0)) AS max_quantity
FROM PRODUCTS p
JOIN SELL_ORDER_DETAILS oi
    ON p.PRODUCT_ID = oi.PRODUCT_ID
GROUP BY
    p.PRODUCT_ID,
    p.PRODUCT_NAME
ORDER BY
    p.PRODUCT_NAME ASC;
