BEGIN
    :SELL_ORDER_MASTER.DISCOUNT_AMOUNT :=
          :SELL_ORDER_MASTER.TOTAL_AMOUNT -NVL(:SELL_ORDER_MASTER.DISCOUNT, 0) ;

    IF :SELL_ORDER_MASTER.DISCOUNT < 0 THEN
        MESSAGE('Discount cannot be negative!');
        RAISE FORM_TRIGGER_FAILURE;
    END IF;
END;
