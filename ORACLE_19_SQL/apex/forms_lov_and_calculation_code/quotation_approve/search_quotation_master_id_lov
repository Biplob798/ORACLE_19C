SELECT DISTINCT 
       M.QUOT_MASTER_ID,
       M.QUOT_DATE
FROM QUOTATION_MASTER M
JOIN QUOTATION_DETAILS D 
     ON M.QUOT_MASTER_ID = D.QUOT_MASTER_ID
WHERE NVL(M.APPROVED, 0) = 0
ORDER BY M.QUOT_MASTER_ID DESC
