SELECT MONTHS_BETWEEN(
         DATE '2026-02-01',
         DATE '2025-02-01'
       ) AS months
FROM dual;



SELECT employee_id,
       FLOOR(MONTHS_BETWEEN(SYSDATE, hire_date) / 12) AS experience_years
FROM employees;
