
-- A Scalar variable holds a single value.

-- It stores basic data types.

DECLARE
    v_salary NUMBER;
    v_name   VARCHAR2(50);
    v_date   DATE;
BEGIN
    v_salary := 5000;
    v_name   := 'Biplob';
    v_date   := SYSDATE;

    DBMS_OUTPUT.PUT_LINE(v_name || ' earns ' || v_salary);
END;
/
